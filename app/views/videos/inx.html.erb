 <head>
  <script src="/assets/video.js"></script>
    <script>
 function popup(pPopup, pYt){ //pYt optional
    $('iframe').attr('src', pYt);
}
       _V_.options.flash.swf = "/assets/video-js.swf"
    </script>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.js" type="text/javascript"></script>
  
  <link href="http://vjs.zencdn.net/4.1/video-js.css" rel="stylesheet">
  <script src="http://vjs.zencdn.net/4.1/video.js"></script>
</head>
      <link href="assets/video-js.css" rel="stylesheet" type="text/css">
  <!-- video.js must be in the <head> for older IEs to work. -->
  <script src="assets/video.js"></script>
    <link href="/assets/video-js.css" rel="stylesheet">
   
<%- model_class = Video -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<!-- <table class="table table-striped table-bordered table-condensed">
  <thead>
    <tr>
      <th class = "success"><% model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:content) %></th>
      <th><% model_class.human_attribute_name(:youtube_link) %></th>
      <th>Video</th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @videos.each do |video| %>
      <tr>
        <td><% link_to video.id, video_path(video) %></td>
        <td><%= video.title %></td>
        <td><%= video.content %></td>
        <td><% video.youtube %></td>
 -->
       <!--   <td> -->
  <% @videos.each do |ad| %>    

  <a href="javascript:void(null)" onclick=<%=video.youtube%> class="more_details">more details</a>
          
                  <% end %>
 
   <iframe width="540" height="320" src=<%=video.content%> frameborder="0" allowfullscreen></iframe> 
 <!-- </td> -->
   <!--  <td><iframe width="640" height="360" src=<%=video.content%> frameborder="0" allowfullscreen></iframe></td>
         <td></td> -->
        <td>
     <!--      <%= link_to t('.edit', :default => t("-helpers.links.edit")),
                      edit_video_path(video), :class => 'btn btn-small' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      video_path(video),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>   -->
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
  
<%  link_to t('.new', :default => t("helpers.links.new")),
            new_video_path,
            :class => 'btn btn-primary' %>
    <a href="javascript:void(null)" onclick="popup('popUpDiv', 'http://www.youtube.com/embed/NWHfY_lvKIQ')" class="more_details">more details</a>