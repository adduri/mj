
<%= form_for(@service) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


     
  <% @u = User.all %>

<% if current_user  %>
    
    <%= f.label :mjid %>
    <input  value="<%= current_user.mj_id%>"  name="service[mjid]" type="hidden">
     <input  value="<%= current_user.mj_id%>" disabled="disabled">
    <%= f.label :phonenumber %>
     <input value="<%= current_user.mobileno %>" name="service[phonenumber]" > 
     <%= f.label :emailid %>
         <input value="<%= current_user.email %>" name="service[emailid]">
<% end %>

  <div class="field">
    
  </div>
  <div class="field">
    
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message , :class =>"input-xlarge span5", :style => "height:180px;"%>
  </div>
  <div class="actions">
        <a href="mailto:saisagarkache@gmail.com">first.last@example.com</a>

    <%= f.submit :class => "btn btn-medium btn-primary" %>
  </div>



<% end %>
